
import React from 'react'

type Props = {
    type: 'FREE' | 'PRO';
    children: React.ReactNode;
}

const SubscriptionPlan = ({type, children}: Props) => {
    // wip : return the subscription plan
  return children;
  
}

export default SubscriptionPlan